=============================================
FogLAMP setpoint notification delivery plugin
=============================================

The *foglamp-notify-control* plugin is designed to allow a notification to call a control operations via the dispatcher service.


